@model PotentialMatchesViewModel

<div>
    <ul>
    @for (var i = 0; i < Model.matches.Count(); i++)
    {
        <li style="color: white; background-color: slateblue;">
            @Html.DisplayFor(model => model.matches[i].Username);
        </li>
    }   
    </ul>
</div>